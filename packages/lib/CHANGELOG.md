# Changelog

All notable changes to `shomer-lib`. Written by [multicz](https://github.com/goabonga/multicz) at release time.

## [0.1.0] - 2026-09-05

### Features

- **lib**: add the shared contracts (`2910b61`)
- **lib**: add the environment-resolved settings (`8131b5a`)
- **lib**: add the system clock (`a7d00f6`)
- **lib**: add the orm models (`c546bbd`)
- **lib**: add the database connector (`1ff47d4`)
- **lib**: add the injection module (`f55ac3d`)
